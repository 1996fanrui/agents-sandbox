name: Publish Coding Runtime

on:
  workflow_dispatch:
    inputs:
      version:
        description: Release version to publish, for example 1.2.3 or v1.2.3
        required: true
        type: string
  push:
    tags:
      - "v*"

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/agents-sandbox/coding-runtime

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve release version
        id: version
        shell: bash
        run: |
          set -e
          if [ "${GITHUB_EVENT_NAME}" = "push" ]; then
            raw_version="${GITHUB_REF_NAME}"
          else
            raw_version="${{ inputs.version }}"
          fi

          version="${raw_version#v}"
          if [ -z "${version}" ]; then
            echo "Release version cannot be empty." >&2
            exit 1
          fi

          echo "version=${version}" >> "${GITHUB_OUTPUT}"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push coding runtime
        uses: docker/build-push-action@v6
        with:
          context: images/coding-runtime
          file: images/coding-runtime/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ steps.version.outputs.version }}
            ${{ env.IMAGE_NAME }}:latest
